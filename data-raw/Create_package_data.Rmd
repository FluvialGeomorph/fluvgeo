---
title: "Create Test Data"
output: html_document
---

```{r}
library(fgm)
library(arcgisbinding)
arc.check_product()
```

# Study Area
Data for tests was taken from the Sinsinawa, IL study site. 

# ESRI File Geodatabase
Test data in ESRI ArcGIS file geodatabase format is stored in 
`fgm/inst/extdata/test.gdb`. 

Convert a file geodatabase feature class to an sp object
```{r}
sin_flowline_points <- fgm::arc2sp("test.gdb/flowline_points")
sin_riffle          <- fgm::arc2sp("test.gdb/riffle")
sin_xs_points       <- fgm::arc2sp("test.gdb/cross_section_points")
sin_xs_dimensions   <- fgm::arc2sp("test.gdb/cross_section_dims")
```

# Run the `usethis::use_data` in the `create_package_steps.R` for each dataset

