% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/terrain_development_report.R
\name{terrain_development_summary}
\alias{terrain_development_summary}
\title{Assemble a pre-Level-1 Terrain Development review}
\usage{
terrain_development_summary(
  study_area = NULL,
  streams = NULL,
  reaches = NULL,
  survey_events = NULL,
  network = NULL,
  dem = NULL,
  terrain_notes = NA_character_,
  analyst_notes = NA_character_
)
}
\arguments{
\item{study_area}{Optional one-row polygon sf with study_area_id and
study_area_name. Its analyst-defined AOI is never inferred from a DEM extent.}

\item{streams}{Optional data frame with stream_id, study_area_id, stream_name.
These are selected Streams; defaults to network Configuration membership.}

\item{reaches}{Optional data frame with reach_id, stream_id, reach_name.
Optional sf geometry represents polygon AOIs, not flowlines.}

\item{survey_events}{Optional data frame with survey_event_id, reach_id,
survey_year and optional survey_month/survey_day. This inventory does not
verify file availability or network-to-event associations.}

\item{network}{Optional Stream Network relation list or fluvgeo GeoPackage path.}

\item{dem}{Optional single-band projected terra SpatRaster. This first slice
displays its extent/grid metadata, not an elevation preview or valid-cell mask.}

\item{terrain_notes}{Supplied terrain source, processing, units and limitations.}

\item{analyst_notes}{Supplied scope, segmentation rationale and next decisions.}
}
\value{
List of tables, map layers, fresh network validation and explicit gaps.
  Inputs and saved network history remain unchanged.
}
\description{
Read-only report data for R and future Shiny clients. Missing inputs remain
explicit; no hierarchy, Survey Event, acceptance, or Level 1 readiness is
inferred. Context tables are report inputs, not new FGDB persistence relations.
}
